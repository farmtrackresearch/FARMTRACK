#pragma once
// ===========================================================================
//  FarmTrack GATEWAY - real credentials.
//  Copy this file to "secrets.h" (same folder) and fill in your real values.
//  secrets.h is gitignored - it will never be committed. This .example file
//  IS committed, as a template for anyone else setting up the project.
// ===========================================================================

static const char* WIFI_SSID          = "YOUR_WIFI_NAME";
static const char* WIFI_PASSWORD      = "YOUR_WIFI_PASSWORD";
static const char* SUPABASE_URL       = "https://your-project.supabase.co";
static const char* SUPABASE_ANON_KEY  = "YOUR_ANON_OR_PUBLISHABLE_KEY";
static const char* GATEWAY_SECRET     = "YOUR_GATEWAY_SECRET"; // the one you hashed into public.gateways
